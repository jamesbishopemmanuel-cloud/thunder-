{
  "name": "veylora",
  "version": "1.0.0",
  "description": "Veylora - Connect, Create, Share. A modern social messaging platform.",
  "main": "index.html",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:build": "npx cap build android",
    "android:build": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1",
    "idb": "^8.0.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/cli": "^6.1.0",
    "@capacitor/android": "^6.1.0"
  }
}
